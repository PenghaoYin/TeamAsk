# TeamAsk

团队 AI 问答空间：支持多人并行向 AI 提问。姓名、会话、消息和 AI 配置统一保存在部署机器的 `data/state.json`，浏览器仅保留用于识别访问者的匿名 Cookie。

每次模型调用的完整请求和响应也会保存到对应会话的调试记录中，其中 API Key 会脱敏。

请运行 `./start.sh` 启动本地页面和 AI 代理。点击右上角设置填写 API 基础地址、端点、Key 和模型名；浏览器请求先发给本地代理，因此目标接口不需要支持 CORS。
